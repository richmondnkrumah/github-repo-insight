{
    "actorOutputSchemaVersion": 1,
    "title": "GitHub Repo Insight Output",
    "properties": {
        "analysis_view": {
            "type": "string",
            "title": "Analysis Overview 🧠",
            "description": "View the AI-generated insights, tech stack, and architecture summary.",
            "template": "{{links.apiDefaultDatasetUrl}}/items?format=json&fields=repo,stars,purpose,architecture_summary,tech_stack,complexity_score"
        },
        "full_data": {
            "type": "string",
            "title": "Full Dataset (JSON) 📦",
            "description": "Download the complete data including the full file tree and raw Readme.",
            "template": "{{links.apiDefaultDatasetUrl}}/items?format=json"
        }
    }
}